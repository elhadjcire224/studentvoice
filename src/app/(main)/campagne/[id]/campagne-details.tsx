import { CampaignHome } from '@/db/queries/campagne.query'
import React from 'react'

export default function CampagneDetails({ campaign }: { campaign: CampaignHome }) {
    return (
        <div>

        </div>
    )
}
